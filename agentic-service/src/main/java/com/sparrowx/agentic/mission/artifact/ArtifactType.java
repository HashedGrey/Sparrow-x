package com.sparrowx.agentic.mission.artifact;

/**
 * Submitted artifact classification aligned with the public proto contract.
 */
public enum ArtifactType {
    UNSPECIFIED,
    DOCUMENT,
    PDF,
    TEXT,
    JSON,
    IMAGE,
    AUDIO,
    VIDEO,
    DATASET
}